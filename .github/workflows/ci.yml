name: CI

on:
  push:
  pull_request:
    branches:
      - main

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build (tsc + vite build)
        run: npm run build

      - name: Upload frontend dist artifact
        uses: actions/upload-artifact@v7
        with:
          name: frontend-dist
          path: frontend/dist

  lint:
    name: lint
    needs: frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      # main.go's `//go:embed all:frontend/dist` requires this directory to
      # exist and be non-empty for ANY Go compilation of the module - even
      # golangci-lint typechecking the root `main` package - so the built
      # frontend from the `frontend` job above is required here too, not
      # just for the actual app build.
      - name: Download frontend dist artifact
        uses: actions/download-artifact@v8
        with:
          name: frontend-dist
          path: frontend/dist

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.12.2

  unit-tests:
    name: unit-tests
    needs: frontend
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      # Same reason as the lint job's download step: `go test ./...`
      # compiles the root `main` package too, which embeds this directory.
      - name: Download frontend dist artifact
        uses: actions/download-artifact@v8
        with:
          name: frontend-dist
          path: frontend/dist

      - name: Run unit tests
        run: go test ./... -race

  integration-tests:
    name: integration-tests
    runs-on: ubuntu-latest
    services:
      minio:
        image: bitnamilegacy/minio:2025.7.23
        env:
          MINIO_ROOT_USER: minioadmin
          MINIO_ROOT_PASSWORD: minioadmin
        ports:
          - 9000:9000
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Wait for MinIO
        run: |
          for i in $(seq 1 30); do
            if curl -fs http://localhost:9000/minio/health/live; then
              echo "MinIO is up"
              exit 0
            fi
            echo "Waiting for MinIO... ($i)"
            sleep 1
          done
          echo "MinIO did not become ready in time" >&2
          exit 1

      - name: Run integration tests
        run: go test -tags=integration ./internal/integration/... -race -count=1
