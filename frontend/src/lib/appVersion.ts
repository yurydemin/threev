/**
 * Single source of truth for the displayed app version, shared by
 * `Sidebar` (footer) and `AboutSection` ("О приложении") — both need the
 * exact same string, so it lives here instead of being duplicated.
 */
export const APP_VERSION = 'v0.1.0';
